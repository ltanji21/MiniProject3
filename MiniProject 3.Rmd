---
title: "Mini Project 3"
author: "Leigh Tanji and Samantha Bryce"
date: "2017年11月15日"
output: html_document
---

```{r}
library(mdsr)
library(RMySQL)
```


Inner joining title and movie kind. Sequels.
```{r}
imdb_sequels <- dbConnect_scidb(dbname = "imdb")
class(imdb_sequels)

imdb_sequels %>%
  dbGetQuery("SELECT DISTINCT title.id, movie_id, title, kind_id, link_type_id
FROM imdb.title
JOIN movie_link
ON title.id = movie_link.movie_id
WHERE (kind_id = '1') 
AND (link_type_id = '1' OR link_type_id = '2');")

```

#Finding Remakes
```{r}
imdb_
SELECT DISTINCT title.id, movie_id, title, kind_id, link_type_id
FROM imdb.title
JOIN movie_link
ON title.id = movie_link.movie_id
WHERE kind_id = 1
AND (link_type_id = 3 OR link_type_id = 4); 
```

#Finding Reboots
```{r}
SELECT DISTINCT title.id, movie_id, title, kind_id, link_type_id
FROM imdb.title
JOIN movie_link
ON title.id = movie_link.movie_id
WHERE (kind_id = '1') 
AND (link_type_id = '1' OR link_type_id = '2'); 
```

































